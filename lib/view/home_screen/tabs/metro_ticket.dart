import 'package:flutter/material.dart';

class MetroTicketScreen extends StatelessWidget {
  const MetroTicketScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}