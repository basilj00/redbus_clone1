import 'package:flutter/material.dart';

class CabTicketScreen extends StatelessWidget {
  const CabTicketScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}